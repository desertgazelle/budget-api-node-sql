const sinon = require("sinon");
require("should");
const categoryController = require("../controllers/categoryController");

describe("categoryController Tests on mocha", () => {
  describe("Post", () => {
    it("should not insert a category with a name already used", () => {
      //arrange
      //act
      //assert
    });
  });
});
